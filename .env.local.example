# Brawlhalla v1 public API key.
# Request one at: https://dev.brawlhalla.com/
# Server-only — never expose to the client.
BRAWLHALLA_API_KEY=

# Supabase Postgres connection strings — grab from the dashboard "Connect" button.
# DATABASE_URL          = Transaction pooler (port 6543). Used at runtime on Vercel.
# DATABASE_URL_UNPOOLED = Session pooler or Direct connection (port 5432). Used by
#   drizzle-kit (db:push) and the node scripts. Both strings carry the DB password.
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Random string used to authenticate Vercel cron invocations.
# Vercel auto-generates one when you add crons; mirror it locally for testing.
CRON_SECRET=

# Supabase Auth (user accounts + claim-your-profile). Public, client-exposed.
# Project Settings -> API. Use the newer "publishable" key (sb_publishable_...),
# not the legacy anon key. Enable Discord + Email providers in the dashboard and
# add http://localhost:3137 + your site URL to the redirect allow-list.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Admin bootstrap. Comma-separated emails always treated as Developer, whatever
# the database says. Admin access is otherwise a property of the account
# (app_users.account_role = 'developer'), which is circular on a fresh install:
# Developer can only be granted from inside the panel, so someone has to get in
# first. Also the way back in if the last Developer is demoted by mistake.
# Env-only on purpose — nothing in the app can write it, so it can't be
# escalated into.
ADMIN_BOOTSTRAP_EMAILS=
